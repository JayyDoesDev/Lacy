import { DefinePlugin, Plugin } from "../../Common/DefinePlugin";

export = DefinePlugin({
  name: "Tags",
  description: "Create tags to use as mini commands in your server!",
  commands: [],
  public_plugin: true
}) as Plugin;
